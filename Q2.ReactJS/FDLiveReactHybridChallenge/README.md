`npm install`

`npm run start`

`N.B. we're using webpack and babel for compilation of the React code, so any updates to the JS/JSX should hot reload. we are using Grunt and Sass for CSS compilation. For more information on either of these:`

`https://gruntjs.com/`

`https://sass-lang.com/`
